"use client";

import type { FC } from "react";
import Typography from "@mui/material/Typography";

const Banner: FC = () => {
	return <Typography variant="h1">GitHub Search</Typography>;
};

export default Banner;
